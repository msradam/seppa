{"connected": "http://pi.local:8000/mcp", "tools": ["step", "reset_session", "fork_at", "list_resources", "read_resource"]}
{"action": "characterize", "is_error": false, "result": {"action": "characterize", "result": {"caps": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}}, "state": {"best_variant": {}, "proposed_variant": {}, "baseline_sps": 0.0, "best_sps": 0.0, "measured_sps": 0.0, "pending_sps": 0.0, "exp_idx": 0, "implement_ok": false, "compile_ok": false, "compile_err": "", "verify_ok": false, "gate_raw": "", "benchmark_done": false, "verdict": "init", "consecutive_no_gain": 0, "variant_log": [], "should_stop": false, "hardware_constraints": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}, "final_summary": {}, "max_experiments": 12}, "valid_next_actions": ["baseline"], "app_id": "518c0fe4-76f1-4799-a6f0-1a4b212798ec", "tracker_project": null, "next_hint": "Reachable actions from current state: baseline.", "next_action_schemas": {"baseline": {}}}}
{"action": "baseline", "is_error": false, "result": {"action": "baseline", "result": {"baseline_steps_per_sec": 1351.3513513513515, "gate_ok": true}, "state": {"best_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Packed-state flux pass: P[i] = (water, surface = terrain + water), so the\n// 10 scalar loads of the split-buffer version become 5 vec2 loads. Physics\n// identical to flux.comp.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x), y = int(gl_GlobalInvocationID.y);\n    if (x >= SZ || y >= SZ) return;\n    uint i = uint(y) * uint(SZ) + uint(x);\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n    vec2 pc_i = P[i];\n    float hi = pc_i.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n    float tot = fL + fR + fU + fD;\n    float wi = pc_i.x;\n    float s = wi / max(tot, max(wi, 1e-20));\n    fL *= s; fR *= s; fU *= s; fD *= s;\n    F[i] = vec4(fL, fR, fU, fD);\n}\n", "height_shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Packed-state height pass. Surface is recomputed exactly from terrain\n// (S' = H + W') rather than accumulated incrementally, so no drift between\n// the water and surface fields over long runs.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x), y = int(gl_GlobalInvocationID.y);\n    if (x >= SZ || y >= SZ) return;\n    uint i = uint(y) * uint(SZ) + uint(x);\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n    uint iL = uint(y) * uint(SZ) + uint(xl);\n    uint iR = uint(y) * uint(SZ) + uint(xr);\n    uint iU = uint(yu) * uint(SZ) + uint(x);\n    uint iD = uint(yd) * uint(SZ) + uint(x);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    float mU = float(y > 0), mD = float(y < SZ - 1);\n    vec4 fi = F[i];\n    float outflow = fi.x + fi.y + fi.z + fi.w;\n    float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n    float w = P[i].x + pc.rain - outflow + inflow;\n    Q[i] = vec2(w, H[i] + w);\n}\n", "strip": 1}, "proposed_variant": {}, "baseline_sps": 1351.3513513513515, "best_sps": 1351.3513513513515, "measured_sps": 0.0, "pending_sps": 0.0, "exp_idx": 0, "implement_ok": false, "compile_ok": false, "compile_err": "", "verify_ok": false, "gate_raw": "", "benchmark_done": false, "verdict": "init", "consecutive_no_gain": 0, "variant_log": [], "should_stop": false, "hardware_constraints": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}, "final_summary": {}, "max_experiments": 12}, "valid_next_actions": ["hypothesize"], "app_id": "518c0fe4-76f1-4799-a6f0-1a4b212798ec", "tracker_project": null, "next_hint": "Reachable actions from current state: hypothesize.", "next_action_schemas": {"hypothesize": {}}}}
{"action": "hypothesize", "is_error": false, "result": {"action": "hypothesize", "result": {"experiment": 1, "best_steps_per_sec": 1351.3513513513515, "best_variant_strip": 1, "best_variant_fused": false, "history": [], "instruction": "Propose a variant via implement inputs: `shader` (flux or fused kernel source), `height_shader` (full source, or empty string for a fused single-dispatch step), `strip` (cells per invocation in y; sets dispatch geometry). All three physics gates must pass. 256 max invocations, 16 KB shared, subgroup 16."}, "state": {"best_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Packed-state flux pass: P[i] = (water, surface = terrain + water), so the\n// 10 scalar loads of the split-buffer version become 5 vec2 loads. Physics\n// identical to flux.comp.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x), y = int(gl_GlobalInvocationID.y);\n    if (x >= SZ || y >= SZ) return;\n    uint i = uint(y) * uint(SZ) + uint(x);\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n    vec2 pc_i = P[i];\n    float hi = pc_i.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n    float tot = fL + fR + fU + fD;\n    float wi = pc_i.x;\n    float s = wi / max(tot, max(wi, 1e-20));\n    fL *= s; fR *= s; fU *= s; fD *= s;\n    F[i] = vec4(fL, fR, fU, fD);\n}\n", "height_shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Packed-state height pass. Surface is recomputed exactly from terrain\n// (S' = H + W') rather than accumulated incrementally, so no drift between\n// the water and surface fields over long runs.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x), y = int(gl_GlobalInvocationID.y);\n    if (x >= SZ || y >= SZ) return;\n    uint i = uint(y) * uint(SZ) + uint(x);\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n    uint iL = uint(y) * uint(SZ) + uint(xl);\n    uint iR = uint(y) * uint(SZ) + uint(xr);\n    uint iU = uint(yu) * uint(SZ) + uint(x);\n    uint iD = uint(yd) * uint(SZ) + uint(x);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    float mU = float(y > 0), mD = float(y < SZ - 1);\n    vec4 fi = F[i];\n    float outflow = fi.x + fi.y + fi.z + fi.w;\n    float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n    float w = P[i].x + pc.rain - outflow + inflow;\n    Q[i] = vec2(w, H[i] + w);\n}\n", "strip": 1}, "proposed_variant": {}, "baseline_sps": 1351.3513513513515, "best_sps": 1351.3513513513515, "measured_sps": 0.0, "pending_sps": 0.0, "exp_idx": 1, "implement_ok": false, "compile_ok": false, "compile_err": "", "verify_ok": false, "gate_raw": "", "benchmark_done": false, "verdict": "init", "consecutive_no_gain": 0, "variant_log": [], "should_stop": false, "hardware_constraints": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}, "final_summary": {}, "max_experiments": 12}, "valid_next_actions": ["implement"], "app_id": "518c0fe4-76f1-4799-a6f0-1a4b212798ec", "tracker_project": null, "next_hint": "Reachable actions from current state: implement.", "next_action_schemas": {"implement": {"shader": {"type": "string", "default": ""}, "height_shader": {"type": "string", "default": ""}, "strip": {"type": "integer", "default": 1}}}}}
{"action": "implement", "is_error": false, "result": {"action": "implement", "result": {"implement_ok": true, "fused": true, "strip": 2}, "state": {"best_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Packed-state flux pass: P[i] = (water, surface = terrain + water), so the\n// 10 scalar loads of the split-buffer version become 5 vec2 loads. Physics\n// identical to flux.comp.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x), y = int(gl_GlobalInvocationID.y);\n    if (x >= SZ || y >= SZ) return;\n    uint i = uint(y) * uint(SZ) + uint(x);\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n    vec2 pc_i = P[i];\n    float hi = pc_i.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n    float tot = fL + fR + fU + fD;\n    float wi = pc_i.x;\n    float s = wi / max(tot, max(wi, 1e-20));\n    fL *= s; fR *= s; fU *= s; fD *= s;\n    F[i] = vec4(fL, fR, fU, fD);\n}\n", "height_shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Packed-state height pass. Surface is recomputed exactly from terrain\n// (S' = H + W') rather than accumulated incrementally, so no drift between\n// the water and surface fields over long runs.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x), y = int(gl_GlobalInvocationID.y);\n    if (x >= SZ || y >= SZ) return;\n    uint i = uint(y) * uint(SZ) + uint(x);\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n    uint iL = uint(y) * uint(SZ) + uint(xl);\n    uint iR = uint(y) * uint(SZ) + uint(xr);\n    uint iU = uint(yu) * uint(SZ) + uint(x);\n    uint iD = uint(yd) * uint(SZ) + uint(x);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    float mU = float(y > 0), mD = float(y < SZ - 1);\n    vec4 fi = F[i];\n    float outflow = fi.x + fi.y + fi.z + fi.w;\n    float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n    float w = P[i].x + pc.rain - outflow + inflow;\n    Q[i] = vec2(w, H[i] + w);\n}\n", "strip": 1}, "proposed_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "baseline_sps": 1351.3513513513515, "best_sps": 1351.3513513513515, "measured_sps": 0.0, "pending_sps": 0.0, "exp_idx": 1, "implement_ok": true, "compile_ok": false, "compile_err": "", "verify_ok": false, "gate_raw": "", "benchmark_done": false, "verdict": "init", "consecutive_no_gain": 0, "variant_log": [], "should_stop": false, "hardware_constraints": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}, "final_summary": {}, "max_experiments": 12}, "valid_next_actions": ["compile_"], "app_id": "518c0fe4-76f1-4799-a6f0-1a4b212798ec", "tracker_project": null, "next_hint": "Reachable actions from current state: compile_.", "next_action_schemas": {"compile_": {}}}}
{"action": "compile_", "is_error": false, "result": {"action": "compile_", "result": {"compile_ok": true, "compile_err": ""}, "state": {"best_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Packed-state flux pass: P[i] = (water, surface = terrain + water), so the\n// 10 scalar loads of the split-buffer version become 5 vec2 loads. Physics\n// identical to flux.comp.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x), y = int(gl_GlobalInvocationID.y);\n    if (x >= SZ || y >= SZ) return;\n    uint i = uint(y) * uint(SZ) + uint(x);\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n    vec2 pc_i = P[i];\n    float hi = pc_i.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n    float tot = fL + fR + fU + fD;\n    float wi = pc_i.x;\n    float s = wi / max(tot, max(wi, 1e-20));\n    fL *= s; fR *= s; fU *= s; fD *= s;\n    F[i] = vec4(fL, fR, fU, fD);\n}\n", "height_shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Packed-state height pass. Surface is recomputed exactly from terrain\n// (S' = H + W') rather than accumulated incrementally, so no drift between\n// the water and surface fields over long runs.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x), y = int(gl_GlobalInvocationID.y);\n    if (x >= SZ || y >= SZ) return;\n    uint i = uint(y) * uint(SZ) + uint(x);\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n    uint iL = uint(y) * uint(SZ) + uint(xl);\n    uint iR = uint(y) * uint(SZ) + uint(xr);\n    uint iU = uint(yu) * uint(SZ) + uint(x);\n    uint iD = uint(yd) * uint(SZ) + uint(x);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    float mU = float(y > 0), mD = float(y < SZ - 1);\n    vec4 fi = F[i];\n    float outflow = fi.x + fi.y + fi.z + fi.w;\n    float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n    float w = P[i].x + pc.rain - outflow + inflow;\n    Q[i] = vec2(w, H[i] + w);\n}\n", "strip": 1}, "proposed_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "baseline_sps": 1351.3513513513515, "best_sps": 1351.3513513513515, "measured_sps": 0.0, "pending_sps": 0.0, "exp_idx": 1, "implement_ok": true, "compile_ok": true, "compile_err": "", "verify_ok": false, "gate_raw": "", "benchmark_done": false, "verdict": "init", "consecutive_no_gain": 0, "variant_log": [], "should_stop": false, "hardware_constraints": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}, "final_summary": {}, "max_experiments": 12}, "valid_next_actions": ["verify"], "app_id": "518c0fe4-76f1-4799-a6f0-1a4b212798ec", "tracker_project": null, "next_hint": "Reachable actions from current state: verify.", "next_action_schemas": {"verify": {}}}}
{"action": "verify", "is_error": false, "result": {"action": "verify", "result": {"verify_ok": true, "steps_per_sec_if_kept": 2127.659574468085}, "state": {"best_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Packed-state flux pass: P[i] = (water, surface = terrain + water), so the\n// 10 scalar loads of the split-buffer version become 5 vec2 loads. Physics\n// identical to flux.comp.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x), y = int(gl_GlobalInvocationID.y);\n    if (x >= SZ || y >= SZ) return;\n    uint i = uint(y) * uint(SZ) + uint(x);\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n    vec2 pc_i = P[i];\n    float hi = pc_i.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n    float tot = fL + fR + fU + fD;\n    float wi = pc_i.x;\n    float s = wi / max(tot, max(wi, 1e-20));\n    fL *= s; fR *= s; fU *= s; fD *= s;\n    F[i] = vec4(fL, fR, fU, fD);\n}\n", "height_shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Packed-state height pass. Surface is recomputed exactly from terrain\n// (S' = H + W') rather than accumulated incrementally, so no drift between\n// the water and surface fields over long runs.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x), y = int(gl_GlobalInvocationID.y);\n    if (x >= SZ || y >= SZ) return;\n    uint i = uint(y) * uint(SZ) + uint(x);\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n    uint iL = uint(y) * uint(SZ) + uint(xl);\n    uint iR = uint(y) * uint(SZ) + uint(xr);\n    uint iU = uint(yu) * uint(SZ) + uint(x);\n    uint iD = uint(yd) * uint(SZ) + uint(x);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    float mU = float(y > 0), mD = float(y < SZ - 1);\n    vec4 fi = F[i];\n    float outflow = fi.x + fi.y + fi.z + fi.w;\n    float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n    float w = P[i].x + pc.rain - outflow + inflow;\n    Q[i] = vec2(w, H[i] + w);\n}\n", "strip": 1}, "proposed_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "baseline_sps": 1351.3513513513515, "best_sps": 1351.3513513513515, "measured_sps": 0.0, "pending_sps": 2127.659574468085, "exp_idx": 1, "implement_ok": true, "compile_ok": true, "compile_err": "", "verify_ok": true, "gate_raw": "selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.188s  3.07 GFLOP/s\ncorrect(NMSE vs CPU)=yes  NMSE=1.43e-11\nmass: rain_injected=524288.0  water_total=524289.8  (conserved vs rain=yes)\npooling: max depth=11.173 at (127,127)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n", "benchmark_done": false, "verdict": "init", "consecutive_no_gain": 0, "variant_log": [], "should_stop": false, "hardware_constraints": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}, "final_summary": {}, "max_experiments": 12}, "valid_next_actions": ["benchmark"], "app_id": "518c0fe4-76f1-4799-a6f0-1a4b212798ec", "tracker_project": null, "next_hint": "Reachable actions from current state: benchmark.", "next_action_schemas": {"benchmark": {}}}}
{"action": "benchmark", "is_error": false, "result": {"action": "benchmark", "result": {"measured_steps_per_sec": 2127.659574468085}, "state": {"best_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Packed-state flux pass: P[i] = (water, surface = terrain + water), so the\n// 10 scalar loads of the split-buffer version become 5 vec2 loads. Physics\n// identical to flux.comp.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x), y = int(gl_GlobalInvocationID.y);\n    if (x >= SZ || y >= SZ) return;\n    uint i = uint(y) * uint(SZ) + uint(x);\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n    vec2 pc_i = P[i];\n    float hi = pc_i.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n    float tot = fL + fR + fU + fD;\n    float wi = pc_i.x;\n    float s = wi / max(tot, max(wi, 1e-20));\n    fL *= s; fR *= s; fU *= s; fD *= s;\n    F[i] = vec4(fL, fR, fU, fD);\n}\n", "height_shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Packed-state height pass. Surface is recomputed exactly from terrain\n// (S' = H + W') rather than accumulated incrementally, so no drift between\n// the water and surface fields over long runs.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x), y = int(gl_GlobalInvocationID.y);\n    if (x >= SZ || y >= SZ) return;\n    uint i = uint(y) * uint(SZ) + uint(x);\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n    uint iL = uint(y) * uint(SZ) + uint(xl);\n    uint iR = uint(y) * uint(SZ) + uint(xr);\n    uint iU = uint(yu) * uint(SZ) + uint(x);\n    uint iD = uint(yd) * uint(SZ) + uint(x);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    float mU = float(y > 0), mD = float(y < SZ - 1);\n    vec4 fi = F[i];\n    float outflow = fi.x + fi.y + fi.z + fi.w;\n    float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n    float w = P[i].x + pc.rain - outflow + inflow;\n    Q[i] = vec2(w, H[i] + w);\n}\n", "strip": 1}, "proposed_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "baseline_sps": 1351.3513513513515, "best_sps": 1351.3513513513515, "measured_sps": 2127.659574468085, "pending_sps": 2127.659574468085, "exp_idx": 1, "implement_ok": true, "compile_ok": true, "compile_err": "", "verify_ok": true, "gate_raw": "selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.188s  3.07 GFLOP/s\ncorrect(NMSE vs CPU)=yes  NMSE=1.43e-11\nmass: rain_injected=524288.0  water_total=524289.8  (conserved vs rain=yes)\npooling: max depth=11.173 at (127,127)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n", "benchmark_done": true, "verdict": "init", "consecutive_no_gain": 0, "variant_log": [], "should_stop": false, "hardware_constraints": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}, "final_summary": {}, "max_experiments": 12}, "valid_next_actions": ["evaluate"], "app_id": "518c0fe4-76f1-4799-a6f0-1a4b212798ec", "tracker_project": null, "next_hint": "Reachable actions from current state: evaluate.", "next_action_schemas": {"evaluate": {}}}}
{"action": "evaluate", "is_error": false, "result": {"action": "evaluate", "result": {"verdict": "keep", "steps_per_sec": 2127.659574468085}, "state": {"best_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "proposed_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "baseline_sps": 1351.3513513513515, "best_sps": 2127.659574468085, "measured_sps": 2127.659574468085, "pending_sps": 2127.659574468085, "exp_idx": 1, "implement_ok": true, "compile_ok": true, "compile_err": "", "verify_ok": true, "gate_raw": "selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.188s  3.07 GFLOP/s\ncorrect(NMSE vs CPU)=yes  NMSE=1.43e-11\nmass: rain_injected=524288.0  water_total=524289.8  (conserved vs rain=yes)\npooling: max depth=11.173 at (127,127)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n", "benchmark_done": true, "verdict": "keep", "consecutive_no_gain": 0, "variant_log": [], "should_stop": false, "hardware_constraints": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}, "final_summary": {}, "max_experiments": 12}, "valid_next_actions": ["log_variant"], "app_id": "518c0fe4-76f1-4799-a6f0-1a4b212798ec", "tracker_project": null, "next_hint": "Reachable actions from current state: log_variant.", "next_action_schemas": {"log_variant": {}}}}
{"action": "log_variant", "is_error": false, "result": {"action": "log_variant", "result": {"logged": {"exp": 1, "fused": true, "strip": 2, "compile_ok": true, "verify_ok": true, "steps_per_sec": 2127.659574468085, "best_sps": 2127.659574468085, "verdict": "keep"}, "consecutive_no_gain": 0}, "state": {"best_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "proposed_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "baseline_sps": 1351.3513513513515, "best_sps": 2127.659574468085, "measured_sps": 2127.659574468085, "pending_sps": 2127.659574468085, "exp_idx": 1, "implement_ok": true, "compile_ok": true, "compile_err": "", "verify_ok": true, "gate_raw": "selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.188s  3.07 GFLOP/s\ncorrect(NMSE vs CPU)=yes  NMSE=1.43e-11\nmass: rain_injected=524288.0  water_total=524289.8  (conserved vs rain=yes)\npooling: max depth=11.173 at (127,127)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n", "benchmark_done": true, "verdict": "keep", "consecutive_no_gain": 0, "variant_log": [{"exp": 1, "fused": true, "strip": 2, "compile_ok": true, "verify_ok": true, "steps_per_sec": 2127.659574468085, "best_sps": 2127.659574468085, "verdict": "keep"}], "should_stop": false, "hardware_constraints": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}, "final_summary": {}, "max_experiments": 12}, "valid_next_actions": ["hypothesize"], "app_id": "518c0fe4-76f1-4799-a6f0-1a4b212798ec", "tracker_project": null, "next_hint": "Reachable actions from current state: hypothesize.", "next_action_schemas": {"hypothesize": {}}}}
{"action": "hypothesize", "is_error": false, "result": {"action": "hypothesize", "result": {"experiment": 2, "best_steps_per_sec": 2127.659574468085, "best_variant_strip": 2, "best_variant_fused": true, "history": [{"exp": 1, "fused": true, "strip": 2, "compile_ok": true, "verify_ok": true, "steps_per_sec": 2127.659574468085, "best_sps": 2127.659574468085, "verdict": "keep"}], "instruction": "Propose a variant via implement inputs: `shader` (flux or fused kernel source), `height_shader` (full source, or empty string for a fused single-dispatch step), `strip` (cells per invocation in y; sets dispatch geometry). All three physics gates must pass. 256 max invocations, 16 KB shared, subgroup 16."}, "state": {"best_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "proposed_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "baseline_sps": 1351.3513513513515, "best_sps": 2127.659574468085, "measured_sps": 2127.659574468085, "pending_sps": 2127.659574468085, "exp_idx": 2, "implement_ok": true, "compile_ok": true, "compile_err": "", "verify_ok": true, "gate_raw": "selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.188s  3.07 GFLOP/s\ncorrect(NMSE vs CPU)=yes  NMSE=1.43e-11\nmass: rain_injected=524288.0  water_total=524289.8  (conserved vs rain=yes)\npooling: max depth=11.173 at (127,127)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n", "benchmark_done": true, "verdict": "keep", "consecutive_no_gain": 0, "variant_log": [{"exp": 1, "fused": true, "strip": 2, "compile_ok": true, "verify_ok": true, "steps_per_sec": 2127.659574468085, "best_sps": 2127.659574468085, "verdict": "keep"}], "should_stop": false, "hardware_constraints": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}, "final_summary": {}, "max_experiments": 12}, "valid_next_actions": ["implement"], "app_id": "518c0fe4-76f1-4799-a6f0-1a4b212798ec", "tracker_project": null, "next_hint": "Reachable actions from current state: implement.", "next_action_schemas": {"implement": {"shader": {"type": "string", "default": ""}, "height_shader": {"type": "string", "default": ""}, "strip": {"type": "integer", "default": 1}}}}}
{"action": "implement", "is_error": false, "result": {"action": "implement", "result": {"implement_ok": true, "fused": true, "strip": 2}, "state": {"best_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "proposed_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + 2.0 * pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "baseline_sps": 1351.3513513513515, "best_sps": 2127.659574468085, "measured_sps": 2127.659574468085, "pending_sps": 2127.659574468085, "exp_idx": 2, "implement_ok": true, "compile_ok": true, "compile_err": "", "verify_ok": true, "gate_raw": "selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.188s  3.07 GFLOP/s\ncorrect(NMSE vs CPU)=yes  NMSE=1.43e-11\nmass: rain_injected=524288.0  water_total=524289.8  (conserved vs rain=yes)\npooling: max depth=11.173 at (127,127)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n", "benchmark_done": true, "verdict": "keep", "consecutive_no_gain": 0, "variant_log": [{"exp": 1, "fused": true, "strip": 2, "compile_ok": true, "verify_ok": true, "steps_per_sec": 2127.659574468085, "best_sps": 2127.659574468085, "verdict": "keep"}], "should_stop": false, "hardware_constraints": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}, "final_summary": {}, "max_experiments": 12}, "valid_next_actions": ["compile_"], "app_id": "518c0fe4-76f1-4799-a6f0-1a4b212798ec", "tracker_project": null, "next_hint": "Reachable actions from current state: compile_.", "next_action_schemas": {"compile_": {}}}}
{"action": "compile_", "is_error": false, "result": {"action": "compile_", "result": {"compile_ok": true, "compile_err": ""}, "state": {"best_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "proposed_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + 2.0 * pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "baseline_sps": 1351.3513513513515, "best_sps": 2127.659574468085, "measured_sps": 2127.659574468085, "pending_sps": 2127.659574468085, "exp_idx": 2, "implement_ok": true, "compile_ok": true, "compile_err": "", "verify_ok": false, "gate_raw": "selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.188s  3.07 GFLOP/s\ncorrect(NMSE vs CPU)=yes  NMSE=1.43e-11\nmass: rain_injected=524288.0  water_total=524289.8  (conserved vs rain=yes)\npooling: max depth=11.173 at (127,127)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n", "benchmark_done": false, "verdict": "keep", "consecutive_no_gain": 0, "variant_log": [{"exp": 1, "fused": true, "strip": 2, "compile_ok": true, "verify_ok": true, "steps_per_sec": 2127.659574468085, "best_sps": 2127.659574468085, "verdict": "keep"}], "should_stop": false, "hardware_constraints": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}, "final_summary": {}, "max_experiments": 12}, "valid_next_actions": ["verify"], "app_id": "518c0fe4-76f1-4799-a6f0-1a4b212798ec", "tracker_project": null, "next_hint": "Reachable actions from current state: verify.", "next_action_schemas": {"verify": {}}}}
{"action": "verify", "is_error": false, "result": {"action": "verify", "result": {"verify_ok": false, "steps_per_sec_if_kept": 2127.659574468085}, "state": {"best_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "proposed_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + 2.0 * pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "baseline_sps": 1351.3513513513515, "best_sps": 2127.659574468085, "measured_sps": 2127.659574468085, "pending_sps": 2127.659574468085, "exp_idx": 2, "implement_ok": true, "compile_ok": true, "compile_err": "", "verify_ok": false, "gate_raw": "selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.188s  3.07 GFLOP/s\ncorrect(NMSE vs CPU)=NO  NMSE=2.25e-01\nmass: rain_injected=524288.0  water_total=786433.6  (conserved vs rain=NO)\npooling: max depth=15.193 at (127,128)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n", "benchmark_done": false, "verdict": "keep", "consecutive_no_gain": 0, "variant_log": [{"exp": 1, "fused": true, "strip": 2, "compile_ok": true, "verify_ok": true, "steps_per_sec": 2127.659574468085, "best_sps": 2127.659574468085, "verdict": "keep"}], "should_stop": false, "hardware_constraints": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}, "final_summary": {}, "max_experiments": 12}, "valid_next_actions": ["log_variant"], "app_id": "518c0fe4-76f1-4799-a6f0-1a4b212798ec", "tracker_project": null, "next_hint": "Reachable actions from current state: log_variant.", "next_action_schemas": {"log_variant": {}}}}
{"action": "benchmark", "is_error": false, "result": {"error": "invalid_transition", "requested": "benchmark", "valid_next_actions": ["log_variant"], "next_action_schemas": {"log_variant": {}}, "message": "action 'benchmark' is not reachable from current state. Valid actions now: ['log_variant'].", "next_hint": "Action 'benchmark' is not reachable from the current state. Reachable now: log_variant."}}
{"gate_demo": "benchmark after failed verify", "server_refused": true, "response": {"error": "invalid_transition", "requested": "benchmark", "valid_next_actions": ["log_variant"], "next_action_schemas": {"log_variant": {}}, "message": "action 'benchmark' is not reachable from current state. Valid actions now: ['log_variant'].", "next_hint": "Action 'benchmark' is not reachable from the current state. Reachable now: log_variant."}}
{"action": "log_variant", "is_error": false, "result": {"action": "log_variant", "result": {"logged": {"exp": 2, "fused": true, "strip": 2, "compile_ok": true, "verify_ok": false, "steps_per_sec": null, "best_sps": 2127.659574468085, "verdict": "revert"}, "consecutive_no_gain": 0}, "state": {"best_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "proposed_variant": {"shader": "#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Fused step, strip of 2: one dispatch per step AND two cells per\n// invocation. Eight flux evaluations per invocation instead of ten for two\n// separate cells (the pair reuses each other's fluxes for their vertical\n// exchange); overlapping P[] loads are CSE'd by the compiler. Dispatch with\n// gy = SZ/32 and no flux buffer.\nvec4 flux4(int cx, int cy, int SZ) {\n    uint i = uint(cy) * uint(SZ) + uint(cx);\n    int xl = max(cx - 1, 0), xr = min(cx + 1, SZ - 1);\n    int yu = max(cy - 1, 0), yd = min(cy + 1, SZ - 1);\n    vec2 p = P[i];\n    float hi = p.y;\n    float fL = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xl)].y));\n    float fR = max(0.0, pc.k * (hi - P[uint(cy) * uint(SZ) + uint(xr)].y));\n    float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(cx)].y));\n    float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(cx)].y));\n    float tot = fL + fR + fU + fD;\n    float s = p.x / max(tot, max(p.x, 1e-20));\n    return vec4(fL, fR, fU, fD) * s;\n}\n\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int ya = int(gl_GlobalInvocationID.y) * 2;\n    if (x >= SZ || ya >= SZ) return;\n    int yb = ya + 1;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    uint iA = uint(ya) * uint(SZ) + uint(x);\n    uint iB = uint(yb) * uint(SZ) + uint(x);\n\n    // Hold only four scalars from the pair's own fluxes (their sums and the\n    // one lane each exchanges with the other); everything else is consumed\n    // as soon as it is produced. The full-vec4 version of this body fails\n    // v3dv register allocation and falls down the strategy ladder.\n    vec4 t = flux4(x, ya, SZ);\n    float outA = t.x + t.y + t.z + t.w, aW = t.w;\n    t = flux4(x, yb, SZ);\n    float outB = t.x + t.y + t.z + t.w, bZ = t.z;\n\n    float inA = flux4(xl, ya, SZ).y * mL\n              + flux4(xr, ya, SZ).x * mR\n              + flux4(x, max(ya - 1, 0), SZ).w * float(ya > 0)\n              + bZ;\n    float wA = P[iA].x + 2.0 * pc.rain - outA + inA;\n    Q[iA] = vec2(wA, H[iA] + wA);\n\n    float inB = flux4(xl, yb, SZ).y * mL\n              + flux4(xr, yb, SZ).x * mR\n              + aW\n              + flux4(x, min(yb + 1, SZ - 1), SZ).z * float(yb < SZ - 1);\n    float wB = P[iB].x + pc.rain - outB + inB;\n    Q[iB] = vec2(wB, H[iB] + wB);\n}\n", "height_shader": "", "strip": 2}, "baseline_sps": 1351.3513513513515, "best_sps": 2127.659574468085, "measured_sps": 2127.659574468085, "pending_sps": 2127.659574468085, "exp_idx": 2, "implement_ok": true, "compile_ok": true, "compile_err": "", "verify_ok": false, "gate_raw": "selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.188s  3.07 GFLOP/s\ncorrect(NMSE vs CPU)=NO  NMSE=2.25e-01\nmass: rain_injected=524288.0  water_total=786433.6  (conserved vs rain=NO)\npooling: max depth=15.193 at (127,128)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n", "benchmark_done": false, "verdict": "revert", "consecutive_no_gain": 0, "variant_log": [{"exp": 1, "fused": true, "strip": 2, "compile_ok": true, "verify_ok": true, "steps_per_sec": 2127.659574468085, "best_sps": 2127.659574468085, "verdict": "keep"}, {"exp": 2, "fused": true, "strip": 2, "compile_ok": true, "verify_ok": false, "steps_per_sec": null, "best_sps": 2127.659574468085, "verdict": "revert"}], "should_stop": false, "hardware_constraints": {"max_wg_invocations": 256, "shared_mem_bytes": 16384, "subgroup_size": 16, "note": "stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"}, "final_summary": {}, "max_experiments": 12}, "valid_next_actions": ["hypothesize"], "app_id": "518c0fe4-76f1-4799-a6f0-1a4b212798ec", "tracker_project": null, "next_hint": "Reachable actions from current state: hypothesize.", "next_action_schemas": {"hypothesize": {}}}}
