[
{"action":"evaluate","result":{"verdict":"keep","steps_per_sec":1470.5882352941176},"state":{"best_variant":{"shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined flux pass: each invocation handles 4 consecutive rows,\n// amortizing the fixed per-invocation launch cost over more stencil work.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        vec2 pc_i = P[i];\n        float hi = pc_i.y;\n        float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n        float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n        float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n        float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n        float tot = fL + fR + fU + fD;\n        float wi = pc_i.x;\n        float ss = wi / max(tot, max(wi, 1e-20));\n        fL *= ss; fR *= ss; fU *= ss; fD *= ss;\n        F[i] = vec4(fL, fR, fU, fD);\n    }\n}\n","height_shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined height pass: same 4-rows-per-invocation strategy as the flux pass.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        uint iL = uint(y) * uint(SZ) + uint(xl);\n        uint iR = uint(y) * uint(SZ) + uint(xr);\n        uint iU = uint(yu) * uint(SZ) + uint(x);\n        uint iD = uint(yd) * uint(SZ) + uint(x);\n        float mU = float(y > 0), mD = float(y < SZ - 1);\n        vec4 fi = F[i];\n        float outflow = fi.x + fi.y + fi.z + fi.w;\n        float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n        float w = P[i].x + pc.rain - outflow + inflow;\n        Q[i] = vec2(w, H[i] + w);\n    }\n}\n","strip":4},"proposed_variant":{"shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined flux pass: each invocation handles 4 consecutive rows,\n// amortizing the fixed per-invocation launch cost over more stencil work.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        vec2 pc_i = P[i];\n        float hi = pc_i.y;\n        float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n        float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n        float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n        float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n        float tot = fL + fR + fU + fD;\n        float wi = pc_i.x;\n        float ss = wi / max(tot, max(wi, 1e-20));\n        fL *= ss; fR *= ss; fU *= ss; fD *= ss;\n        F[i] = vec4(fL, fR, fU, fD);\n    }\n}\n","height_shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined height pass: same 4-rows-per-invocation strategy as the flux pass.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        uint iL = uint(y) * uint(SZ) + uint(xl);\n        uint iR = uint(y) * uint(SZ) + uint(xr);\n        uint iU = uint(yu) * uint(SZ) + uint(x);\n        uint iD = uint(yd) * uint(SZ) + uint(x);\n        float mU = float(y > 0), mD = float(y < SZ - 1);\n        vec4 fi = F[i];\n        float outflow = fi.x + fi.y + fi.z + fi.w;\n        float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n        float w = P[i].x + pc.rain - outflow + inflow;\n        Q[i] = vec2(w, H[i] + w);\n    }\n}\n","strip":4},"baseline_sps":1351.3513513513515,"best_sps":1470.5882352941176,"measured_sps":1470.5882352941176,"pending_sps":1470.5882352941176,"exp_idx":1,"implement_ok":true,"compile_ok":true,"compile_err":"","verify_ok":true,"gate_raw":"selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.272s  2.12 GFLOP/s\ncorrect(NMSE vs CPU)=yes  NMSE=1.46e-11\nmass: rain_injected=524288.0  water_total=524289.8  (conserved vs rain=yes)\npooling: max depth=11.173 at (127,127)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n","benchmark_done":true,"verdict":"keep","consecutive_no_gain":0,"variant_log":[],"should_stop":false,"hardware_constraints":{"max_wg_invocations":256,"shared_mem_bytes":16384,"subgroup_size":16,"note":"stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"},"final_summary":{},"max_experiments":12},"valid_next_actions":["log_variant"],"app_id":"7d2d0e3d-9ad7-4a10-9ee8-2767d3d3246e","tracker_project":null,"next_hint":"Reachable actions from current state: log_variant.","next_action_schemas":{"log_variant":{}}},
{"action":"log_variant","result":{"logged":{"exp":1,"fused":false,"strip":4,"compile_ok":true,"verify_ok":true,"steps_per_sec":1470.5882352941176,"best_sps":1470.5882352941176,"verdict":"keep"},"consecutive_no_gain":0},"state":{"best_variant":{"shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined flux pass: each invocation handles 4 consecutive rows,\n// amortizing the fixed per-invocation launch cost over more stencil work.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        vec2 pc_i = P[i];\n        float hi = pc_i.y;\n        float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n        float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n        float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n        float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n        float tot = fL + fR + fU + fD;\n        float wi = pc_i.x;\n        float ss = wi / max(tot, max(wi, 1e-20));\n        fL *= ss; fR *= ss; fU *= ss; fD *= ss;\n        F[i] = vec4(fL, fR, fU, fD);\n    }\n}\n","height_shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined height pass: same 4-rows-per-invocation strategy as the flux pass.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        uint iL = uint(y) * uint(SZ) + uint(xl);\n        uint iR = uint(y) * uint(SZ) + uint(xr);\n        uint iU = uint(yu) * uint(SZ) + uint(x);\n        uint iD = uint(yd) * uint(SZ) + uint(x);\n        float mU = float(y > 0), mD = float(y < SZ - 1);\n        vec4 fi = F[i];\n        float outflow = fi.x + fi.y + fi.z + fi.w;\n        float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n        float w = P[i].x + pc.rain - outflow + inflow;\n        Q[i] = vec2(w, H[i] + w);\n    }\n}\n","strip":4},"proposed_variant":{"shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined flux pass: each invocation handles 4 consecutive rows,\n// amortizing the fixed per-invocation launch cost over more stencil work.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        vec2 pc_i = P[i];\n        float hi = pc_i.y;\n        float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n        float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n        float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n        float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n        float tot = fL + fR + fU + fD;\n        float wi = pc_i.x;\n        float ss = wi / max(tot, max(wi, 1e-20));\n        fL *= ss; fR *= ss; fU *= ss; fD *= ss;\n        F[i] = vec4(fL, fR, fU, fD);\n    }\n}\n","height_shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined height pass: same 4-rows-per-invocation strategy as the flux pass.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        uint iL = uint(y) * uint(SZ) + uint(xl);\n        uint iR = uint(y) * uint(SZ) + uint(xr);\n        uint iU = uint(yu) * uint(SZ) + uint(x);\n        uint iD = uint(yd) * uint(SZ) + uint(x);\n        float mU = float(y > 0), mD = float(y < SZ - 1);\n        vec4 fi = F[i];\n        float outflow = fi.x + fi.y + fi.z + fi.w;\n        float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n        float w = P[i].x + pc.rain - outflow + inflow;\n        Q[i] = vec2(w, H[i] + w);\n    }\n}\n","strip":4},"baseline_sps":1351.3513513513515,"best_sps":1470.5882352941176,"measured_sps":1470.5882352941176,"pending_sps":1470.5882352941176,"exp_idx":1,"implement_ok":true,"compile_ok":true,"compile_err":"","verify_ok":true,"gate_raw":"selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.272s  2.12 GFLOP/s\ncorrect(NMSE vs CPU)=yes  NMSE=1.46e-11\nmass: rain_injected=524288.0  water_total=524289.8  (conserved vs rain=yes)\npooling: max depth=11.173 at (127,127)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n","benchmark_done":true,"verdict":"keep","consecutive_no_gain":0,"variant_log":[{"exp":1,"fused":false,"strip":4,"compile_ok":true,"verify_ok":true,"steps_per_sec":1470.5882352941176,"best_sps":1470.5882352941176,"verdict":"keep"}],"should_stop":false,"hardware_constraints":{"max_wg_invocations":256,"shared_mem_bytes":16384,"subgroup_size":16,"note":"stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"},"final_summary":{},"max_experiments":12},"valid_next_actions":["hypothesize"],"app_id":"7d2d0e3d-9ad7-4a10-9ee8-2767d3d3246e","tracker_project":null,"next_hint":"Reachable actions from current state: hypothesize.","next_action_schemas":{"hypothesize":{}}},
{"action":"evaluate","result":{"verdict":"revert","steps_per_sec":921.6589861751152,"best":1470.5882352941176},"state":{"best_variant":{"shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined flux pass: each invocation handles 4 consecutive rows,\n// amortizing the fixed per-invocation launch cost over more stencil work.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        vec2 pc_i = P[i];\n        float hi = pc_i.y;\n        float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n        float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n        float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n        float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n        float tot = fL + fR + fU + fD;\n        float wi = pc_i.x;\n        float ss = wi / max(tot, max(wi, 1e-20));\n        fL *= ss; fR *= ss; fU *= ss; fD *= ss;\n        F[i] = vec4(fL, fR, fU, fD);\n    }\n}\n","height_shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined height pass: same 4-rows-per-invocation strategy as the flux pass.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        uint iL = uint(y) * uint(SZ) + uint(xl);\n        uint iR = uint(y) * uint(SZ) + uint(xr);\n        uint iU = uint(yu) * uint(SZ) + uint(x);\n        uint iD = uint(yd) * uint(SZ) + uint(x);\n        float mU = float(y > 0), mD = float(y < SZ - 1);\n        vec4 fi = F[i];\n        float outflow = fi.x + fi.y + fi.z + fi.w;\n        float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n        float w = P[i].x + pc.rain - outflow + inflow;\n        Q[i] = vec2(w, H[i] + w);\n    }\n}\n","strip":4},"proposed_variant":{"shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined flux pass: each invocation handles 8 consecutive rows,\n// amortizing the fixed per-invocation launch cost over more stencil work.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 8;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    for (int s = 0; s < 8; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        vec2 pc_i = P[i];\n        float hi = pc_i.y;\n        float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n        float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n        float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n        float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n        float tot = fL + fR + fU + fD;\n        float wi = pc_i.x;\n        float ss = wi / max(tot, max(wi, 1e-20));\n        fL *= ss; fR *= ss; fU *= ss; fD *= ss;\n        F[i] = vec4(fL, fR, fU, fD);\n    }\n}\n","height_shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined height pass: same 8-rows-per-invocation strategy as the flux pass.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 8;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    for (int s = 0; s < 8; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        uint iL = uint(y) * uint(SZ) + uint(xl);\n        uint iR = uint(y) * uint(SZ) + uint(xr);\n        uint iU = uint(yu) * uint(SZ) + uint(x);\n        uint iD = uint(yd) * uint(SZ) + uint(x);\n        float mU = float(y > 0), mD = float(y < SZ - 1);\n        vec4 fi = F[i];\n        float outflow = fi.x + fi.y + fi.z + fi.w;\n        float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n        float w = P[i].x + pc.rain - outflow + inflow;\n        Q[i] = vec2(w, H[i] + w);\n    }\n}\n","strip":8},"baseline_sps":1351.3513513513515,"best_sps":1470.5882352941176,"measured_sps":921.6589861751152,"pending_sps":921.6589861751152,"exp_idx":2,"implement_ok":true,"compile_ok":true,"compile_err":"","verify_ok":true,"gate_raw":"selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.434s  1.33 GFLOP/s\ncorrect(NMSE vs CPU)=yes  NMSE=1.46e-11\nmass: rain_injected=524288.0  water_total=524289.8  (conserved vs rain=yes)\npooling: max depth=11.173 at (127,127)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n","benchmark_done":true,"verdict":"revert","consecutive_no_gain":1,"variant_log":[{"exp":1,"fused":false,"strip":4,"compile_ok":true,"verify_ok":true,"steps_per_sec":1470.5882352941176,"best_sps":1470.5882352941176,"verdict":"keep"}],"should_stop":false,"hardware_constraints":{"max_wg_invocations":256,"shared_mem_bytes":16384,"subgroup_size":16,"note":"stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"},"final_summary":{},"max_experiments":12},"valid_next_actions":["log_variant"],"app_id":"7d2d0e3d-9ad7-4a10-9ee8-2767d3d3246e","tracker_project":null,"next_hint":"Reachable actions from current state: log_variant.","next_action_schemas":{"log_variant":{}}},
{"action":"log_variant","result":{"logged":{"exp":2,"fused":false,"strip":8,"compile_ok":true,"verify_ok":true,"steps_per_sec":921.6589861751152,"best_sps":1470.5882352941176,"verdict":"revert"},"consecutive_no_gain":1},"state":{"best_variant":{"shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined flux pass: each invocation handles 4 consecutive rows,\n// amortizing the fixed per-invocation launch cost over more stencil work.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        vec2 pc_i = P[i];\n        float hi = pc_i.y;\n        float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n        float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n        float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n        float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n        float tot = fL + fR + fU + fD;\n        float wi = pc_i.x;\n        float ss = wi / max(tot, max(wi, 1e-20));\n        fL *= ss; fR *= ss; fU *= ss; fD *= ss;\n        F[i] = vec4(fL, fR, fU, fD);\n    }\n}\n","height_shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined height pass: same 4-rows-per-invocation strategy as the flux pass.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        uint iL = uint(y) * uint(SZ) + uint(xl);\n        uint iR = uint(y) * uint(SZ) + uint(xr);\n        uint iU = uint(yu) * uint(SZ) + uint(x);\n        uint iD = uint(yd) * uint(SZ) + uint(x);\n        float mU = float(y > 0), mD = float(y < SZ - 1);\n        vec4 fi = F[i];\n        float outflow = fi.x + fi.y + fi.z + fi.w;\n        float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n        float w = P[i].x + pc.rain - outflow + inflow;\n        Q[i] = vec2(w, H[i] + w);\n    }\n}\n","strip":4},"proposed_variant":{"shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined flux pass: each invocation handles 8 consecutive rows,\n// amortizing the fixed per-invocation launch cost over more stencil work.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 8;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    for (int s = 0; s < 8; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        vec2 pc_i = P[i];\n        float hi = pc_i.y;\n        float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n        float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n        float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n        float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n        float tot = fL + fR + fU + fD;\n        float wi = pc_i.x;\n        float ss = wi / max(tot, max(wi, 1e-20));\n        fL *= ss; fR *= ss; fU *= ss; fD *= ss;\n        F[i] = vec4(fL, fR, fU, fD);\n    }\n}\n","height_shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined height pass: same 8-rows-per-invocation strategy as the flux pass.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 8;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    for (int s = 0; s < 8; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        uint iL = uint(y) * uint(SZ) + uint(xl);\n        uint iR = uint(y) * uint(SZ) + uint(xr);\n        uint iU = uint(yu) * uint(SZ) + uint(x);\n        uint iD = uint(yd) * uint(SZ) + uint(x);\n        float mU = float(y > 0), mD = float(y < SZ - 1);\n        vec4 fi = F[i];\n        float outflow = fi.x + fi.y + fi.z + fi.w;\n        float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n        float w = P[i].x + pc.rain - outflow + inflow;\n        Q[i] = vec2(w, H[i] + w);\n    }\n}\n","strip":8},"baseline_sps":1351.3513513513515,"best_sps":1470.5882352941176,"measured_sps":921.6589861751152,"pending_sps":921.6589861751152,"exp_idx":2,"implement_ok":true,"compile_ok":true,"compile_err":"","verify_ok":true,"gate_raw":"selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.434s  1.33 GFLOP/s\ncorrect(NMSE vs CPU)=yes  NMSE=1.46e-11\nmass: rain_injected=524288.0  water_total=524289.8  (conserved vs rain=yes)\npooling: max depth=11.173 at (127,127)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n","benchmark_done":true,"verdict":"revert","consecutive_no_gain":1,"variant_log":[{"exp":1,"fused":false,"strip":4,"compile_ok":true,"verify_ok":true,"steps_per_sec":1470.5882352941176,"best_sps":1470.5882352941176,"verdict":"keep"},{"exp":2,"fused":false,"strip":8,"compile_ok":true,"verify_ok":true,"steps_per_sec":921.6589861751152,"best_sps":1470.5882352941176,"verdict":"revert"}],"should_stop":false,"hardware_constraints":{"max_wg_invocations":256,"shared_mem_bytes":16384,"subgroup_size":16,"note":"stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"},"final_summary":{},"max_experiments":12},"valid_next_actions":["hypothesize"],"app_id":"7d2d0e3d-9ad7-4a10-9ee8-2767d3d3246e","tracker_project":null,"next_hint":"Reachable actions from current state: hypothesize.","next_action_schemas":{"hypothesize":{}}},
{"action":"evaluate","result":{"verdict":"revert","steps_per_sec":1298.7012987012988,"best":1470.5882352941176},"state":{"best_variant":{"shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined flux pass: each invocation handles 4 consecutive rows,\n// amortizing the fixed per-invocation launch cost over more stencil work.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        vec2 pc_i = P[i];\n        float hi = pc_i.y;\n        float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n        float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n        float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n        float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n        float tot = fL + fR + fU + fD;\n        float wi = pc_i.x;\n        float ss = wi / max(tot, max(wi, 1e-20));\n        fL *= ss; fR *= ss; fU *= ss; fD *= ss;\n        F[i] = vec4(fL, fR, fU, fD);\n    }\n}\n","height_shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined height pass: same 4-rows-per-invocation strategy as the flux pass.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        uint iL = uint(y) * uint(SZ) + uint(xl);\n        uint iR = uint(y) * uint(SZ) + uint(xr);\n        uint iU = uint(yu) * uint(SZ) + uint(x);\n        uint iD = uint(yd) * uint(SZ) + uint(x);\n        float mU = float(y > 0), mD = float(y < SZ - 1);\n        vec4 fi = F[i];\n        float outflow = fi.x + fi.y + fi.z + fi.w;\n        float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n        float w = P[i].x + pc.rain - outflow + inflow;\n        Q[i] = vec2(w, H[i] + w);\n    }\n}\n","strip":4},"proposed_variant":{"shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined flux pass: each invocation handles 6 consecutive rows,\n// amortizing the fixed per-invocation launch cost over more stencil work.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 6;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    for (int s = 0; s < 6; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        vec2 pc_i = P[i];\n        float hi = pc_i.y;\n        float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n        float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n        float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n        float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n        float tot = fL + fR + fU + fD;\n        float wi = pc_i.x;\n        float ss = wi / max(tot, max(wi, 1e-20));\n        fL *= ss; fR *= ss; fU *= ss; fD *= ss;\n        F[i] = vec4(fL, fR, fU, fD);\n    }\n}\n","height_shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined height pass: same 6-rows-per-invocation strategy as the flux pass.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 6;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    for (int s = 0; s < 6; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        uint iL = uint(y) * uint(SZ) + uint(xl);\n        uint iR = uint(y) * uint(SZ) + uint(xr);\n        uint iU = uint(yu) * uint(SZ) + uint(x);\n        uint iD = uint(yd) * uint(SZ) + uint(x);\n        float mU = float(y > 0), mD = float(y < SZ - 1);\n        vec4 fi = F[i];\n        float outflow = fi.x + fi.y + fi.z + fi.w;\n        float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n        float w = P[i].x + pc.rain - outflow + inflow;\n        Q[i] = vec2(w, H[i] + w);\n    }\n}\n","strip":6},"baseline_sps":1351.3513513513515,"best_sps":1470.5882352941176,"measured_sps":1298.7012987012988,"pending_sps":1298.7012987012988,"exp_idx":3,"implement_ok":true,"compile_ok":true,"compile_err":"","verify_ok":true,"gate_raw":"selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.308s  1.87 GFLOP/s\ncorrect(NMSE vs CPU)=yes  NMSE=1.46e-11\nmass: rain_injected=524288.0  water_total=524289.8  (conserved vs rain=yes)\npooling: max depth=11.173 at (127,127)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n","benchmark_done":true,"verdict":"revert","consecutive_no_gain":2,"variant_log":[{"exp":1,"fused":false,"strip":4,"compile_ok":true,"verify_ok":true,"steps_per_sec":1470.5882352941176,"best_sps":1470.5882352941176,"verdict":"keep"},{"exp":2,"fused":false,"strip":8,"compile_ok":true,"verify_ok":true,"steps_per_sec":921.6589861751152,"best_sps":1470.5882352941176,"verdict":"revert"}],"should_stop":false,"hardware_constraints":{"max_wg_invocations":256,"shared_mem_bytes":16384,"subgroup_size":16,"note":"stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"},"final_summary":{},"max_experiments":12},"valid_next_actions":["log_variant"],"app_id":"7d2d0e3d-9ad7-4a10-9ee8-2767d3d3246e","tracker_project":null,"next_hint":"Reachable actions from current state: log_variant.","next_action_schemas":{"log_variant":{}}},
{"action":"log_variant","result":{"logged":{"exp":3,"fused":false,"strip":6,"compile_ok":true,"verify_ok":true,"steps_per_sec":1298.7012987012988,"best_sps":1470.5882352941176,"verdict":"revert"},"consecutive_no_gain":2},"state":{"best_variant":{"shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined flux pass: each invocation handles 4 consecutive rows,\n// amortizing the fixed per-invocation launch cost over more stencil work.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        vec2 pc_i = P[i];\n        float hi = pc_i.y;\n        float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n        float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n        float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n        float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n        float tot = fL + fR + fU + fD;\n        float wi = pc_i.x;\n        float ss = wi / max(tot, max(wi, 1e-20));\n        fL *= ss; fR *= ss; fU *= ss; fD *= ss;\n        F[i] = vec4(fL, fR, fU, fD);\n    }\n}\n","height_shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined height pass: same 4-rows-per-invocation strategy as the flux pass.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 4;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    for (int s = 0; s < 4; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        uint iL = uint(y) * uint(SZ) + uint(xl);\n        uint iR = uint(y) * uint(SZ) + uint(xr);\n        uint iU = uint(yu) * uint(SZ) + uint(x);\n        uint iD = uint(yd) * uint(SZ) + uint(x);\n        float mU = float(y > 0), mD = float(y < SZ - 1);\n        vec4 fi = F[i];\n        float outflow = fi.x + fi.y + fi.z + fi.w;\n        float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n        float w = P[i].x + pc.rain - outflow + inflow;\n        Q[i] = vec2(w, H[i] + w);\n    }\n}\n","strip":4},"proposed_variant":{"shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) writeonly buffer Flux { vec4 F[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined flux pass: each invocation handles 6 consecutive rows,\n// amortizing the fixed per-invocation launch cost over more stencil work.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 6;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    for (int s = 0; s < 6; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        vec2 pc_i = P[i];\n        float hi = pc_i.y;\n        float fL = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xl)].y));\n        float fR = max(0.0, pc.k * (hi - P[uint(y) * uint(SZ) + uint(xr)].y));\n        float fU = max(0.0, pc.k * (hi - P[uint(yu) * uint(SZ) + uint(x)].y));\n        float fD = max(0.0, pc.k * (hi - P[uint(yd) * uint(SZ) + uint(x)].y));\n        float tot = fL + fR + fU + fD;\n        float wi = pc_i.x;\n        float ss = wi / max(tot, max(wi, 1e-20));\n        fL *= ss; fR *= ss; fU *= ss; fD *= ss;\n        F[i] = vec4(fL, fR, fU, fD);\n    }\n}\n","height_shader":"#version 450\nlayout(local_size_x = 16, local_size_y = 16) in;\nlayout(std430, binding = 0) readonly buffer Terrain { float H[]; };\nlayout(std430, binding = 1) readonly buffer PIn { vec2 P[]; };\nlayout(std430, binding = 2) readonly buffer Flux { vec4 F[]; };\nlayout(std430, binding = 3) writeonly buffer POut { vec2 Q[]; };\nlayout(push_constant) uniform PC { uint SZ; float k; float rain; } pc;\n\n// Strip-mined height pass: same 6-rows-per-invocation strategy as the flux pass.\nvoid main() {\n    int SZ = int(pc.SZ);\n    int x = int(gl_GlobalInvocationID.x);\n    int y0 = int(gl_GlobalInvocationID.y) * 6;\n    if (x >= SZ) return;\n    int xl = max(x - 1, 0), xr = min(x + 1, SZ - 1);\n    float mL = float(x > 0), mR = float(x < SZ - 1);\n    for (int s = 0; s < 6; ++s) {\n        int y = y0 + s;\n        if (y >= SZ) break;\n        uint i = uint(y) * uint(SZ) + uint(x);\n        int yu = max(y - 1, 0), yd = min(y + 1, SZ - 1);\n        uint iL = uint(y) * uint(SZ) + uint(xl);\n        uint iR = uint(y) * uint(SZ) + uint(xr);\n        uint iU = uint(yu) * uint(SZ) + uint(x);\n        uint iD = uint(yd) * uint(SZ) + uint(x);\n        float mU = float(y > 0), mD = float(y < SZ - 1);\n        vec4 fi = F[i];\n        float outflow = fi.x + fi.y + fi.z + fi.w;\n        float inflow = F[iL].y * mL + F[iR].x * mR + F[iU].w * mU + F[iD].z * mD;\n        float w = P[i].x + pc.rain - outflow + inflow;\n        Q[i] = vec2(w, H[i] + w);\n    }\n}\n","strip":6},"baseline_sps":1351.3513513513515,"best_sps":1470.5882352941176,"measured_sps":1298.7012987012988,"pending_sps":1298.7012987012988,"exp_idx":3,"implement_ok":true,"compile_ok":true,"compile_err":"","verify_ok":true,"gate_raw":"selected: V3D 7.1.10.2\ngrid=256^2  steps=400  time=0.308s  1.87 GFLOP/s\ncorrect(NMSE vs CPU)=yes  NMSE=1.46e-11\nmass: rain_injected=524288.0  water_total=524289.8  (conserved vs rain=yes)\npooling: max depth=11.173 at (127,127)  basin_centre=(128,128)  (pools in basin=yes)\ndumped /tmp/flood_depth.txt (16x16 depth grid)\n","benchmark_done":true,"verdict":"revert","consecutive_no_gain":2,"variant_log":[{"exp":1,"fused":false,"strip":4,"compile_ok":true,"verify_ok":true,"steps_per_sec":1470.5882352941176,"best_sps":1470.5882352941176,"verdict":"keep"},{"exp":2,"fused":false,"strip":8,"compile_ok":true,"verify_ok":true,"steps_per_sec":921.6589861751152,"best_sps":1470.5882352941176,"verdict":"revert"},{"exp":3,"fused":false,"strip":6,"compile_ok":true,"verify_ok":true,"steps_per_sec":1298.7012987012988,"best_sps":1470.5882352941176,"verdict":"revert"}],"should_stop":false,"hardware_constraints":{"max_wg_invocations":256,"shared_mem_bytes":16384,"subgroup_size":16,"note":"stencil is bound by fixed per-invocation cost; host contract (fused, strip) is part of the search space, not just shader bodies"},"final_summary":{},"max_experiments":12},"valid_next_actions":["hypothesize"],"app_id":"7d2d0e3d-9ad7-4a10-9ee8-2767d3d3246e","tracker_project":null,"next_hint":"Reachable actions from current state: hypothesize.","next_action_schemas":{"hypothesize":{}}}
]
